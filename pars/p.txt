$foo = 3;  # "hello;"

if ($x + ($y . 3) - 4) {
   $a = $b;
}

sub sq {
    my ($x) = @_;
    $k += $a ? $b : 4;
    return $x * $x;
}
my $s = "hi # {} \"hey\"  ";

if ($k =~ /foo/ || $w =~ m,zzz, || $u eq $v) {
    $u .= "aa";
    system_or_die "foo";
    print $j;
}
else {
    for my $x (@a) {
        push @b, f($x);
}